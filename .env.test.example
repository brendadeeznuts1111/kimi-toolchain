# Copy to .env.test for local test-only variables (loaded via bun test --env-file).
# Do NOT set NODE_ENV here — kimi-toolchain forces NODE_ENV=test in test/setup.ts.
# KIMI_TEST_API_URL=http://127.0.0.1:9999