# CLI naming notes

- `kimi-doctor --session-report` is **deprecated** — use `--effect-floor` (effect-discipline floor counts, not Herdr session health).
- `herdr-doctor` reports machine/DX Herdr integration health; it has no `--session-report` or `--effect-floor` flag.