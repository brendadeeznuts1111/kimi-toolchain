# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "kimi-toolchain"

[setup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

echo "→ Ensuring frozen lockfile + SSOT contract..."
bun install --frozen-lockfile

# Optional: warm the serve-probe cache / build the diagnostic envelope
# bun run serve-probe:build || true

echo "→ kimi-toolchain worktree ready for Artifact Portal convergence."
'''

[cleanup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

echo "→ Cleaning node_modules + .turbo/.next caches for clean handoff..."
rm -rf node_modules examples/*/node_modules
bun pm cache rm || true
'''
