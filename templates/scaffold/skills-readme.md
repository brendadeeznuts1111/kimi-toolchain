# Project skills

Place project-local Kimi Code skills in `.kimi-code/skills/<name>/SKILL.md`.

User-wide skills (after `bun run sync` from kimi-toolchain):

- `~/.kimi-code/skills/` — Kimi Code skill discovery
- `~/.agents/skills/` — Cursor/Codex skill copy

Bundled skills synced from kimi-toolchain `skills/`: `kimi-toolchain`, `cloudflare-access`, `herdr` (3 total).

Runtime map and path layout: `~/.kimi-code/UNIFIED.md` (repo `UNIFIED.md`).

Tool failure taxonomy: `~/.kimi-code/error-taxonomy.yml`.
