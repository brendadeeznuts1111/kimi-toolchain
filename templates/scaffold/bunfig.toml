# Install policy — https://bun.com/docs/pm/cli/install#configuring-bun-install-with-bunfig-toml
# Search order (merged): $XDG_CONFIG_HOME/.bunfig.toml → $HOME/.bunfig.toml → ./bunfig.toml
# BUN_CONFIG_* env overrides bunfig. Dep changes: `bun add` / `bun update`. Trust: package.json `trustedDependencies`.
[install]
optional = true
dev = true
peer = true
production = false
saveTextLockfile = true
frozenLockfile = true
dryRun = false
ignoreScripts = false
linker = "isolated"
globalDir = "~/.bun/install/global"
globalBinDir = "~/.bun/bin"
minimumReleaseAge = 259200
minimumReleaseAgeExcludes = ["@types/bun", "@types/node", "typescript"]

[test]
concurrentTestGlob = ["test/*.unit.test.ts"]
coverageSkipTestFiles = true

coverageThreshold = { lines = 0.35, functions = 0.25 }
