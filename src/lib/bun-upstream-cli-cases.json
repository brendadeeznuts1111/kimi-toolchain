{
  "test/cli/bun.test.ts": [
    "respects NO_COLOR=${JSON.stringify(value)} to disable color",
    "revision generates version numbers correctly",
    "getcompletes should not panic and should not be empty",
    "getcompletes keeps scripts whose names start with ",
    "test --config, issue #4128",
    "[todo] respects NO_COLOR=${JSON.stringify(value)} to enable color"
  ],
  "test/cli/bunfig-test-options.test.ts": [
    "randomize with seed produces consistent order",
    "alpha",
    "bravo",
    "charlie",
    "delta",
    "echo",
    "seed without randomize errors",
    "test 1",
    "seed with randomize=false errors",
    "rerunEach option works",
    "all test options together",
    "test 2"
  ],
  "test/cli/console-depth.test.ts": [
    "default console depth should be 2",
    "--console-depth flag sets custom depth",
    "--console-depth with higher value shows deeper nesting",
    "bunfig.toml console.depth configuration",
    "CLI flag overrides bunfig.toml",
    "invalid --console-depth value shows error",
    "edge case: depth 0 should show infinite depth",
    "bunfig.toml depth=0 should show infinite depth",
    "console depth affects console.log, console.error, and console.warn"
  ],
  "test/cli/create/create-jsx.test.ts": [
    "auto-install passes detected dependencies as positionals"
  ],
  "test/cli/env/bun-options.test.ts": [
    "basic usage - passes options to bun command",
    "multiple options - passes all options to bun command",
    "options with quotes - properly handles quoted options",
    "priority - environment options go before command line options",
    "bare flag before flag with value is recognized",
    "bare flag before flag with value is recognized (standalone executable)",
    "empty BUN_OPTIONS - should work normally"
  ],
  "test/cli/env/ci-info.test.ts": [
    "Without CI env vars, test.only should work",
    "CI=false disables CI detection even with GITHUB_ACTIONS=true",
    "CI=true enables CI detection even with no CI env vars",
    "CI=true enables CI detection with GITHUB_ACTIONS=true"
  ],
  "test/cli/heap-prof.test.ts": [
    "--heap-prof generates V8 heap snapshot on exit",
    "--heap-prof-md generates markdown heap profile on exit",
    "--heap-prof-dir specifies output directory for V8 format",
    "--heap-prof-dir specifies output directory for markdown format",
    "--heap-prof-name specifies output filename",
    "--heap-prof-name and --heap-prof-dir work together",
    "--heap-prof-name without --heap-prof or --heap-prof-md shows warning"
  ],
  "test/cli/hot/hot.test.ts": [
    "preload not found should exit with code 1 and not time out",
    "should hot reload when file is overwritten",
    "should recover from errors",
    "should not hot reload when a random file is written",
    "should hot reload when a file is deleted and rewritten",
    "should hot reload when a file is renamed() into place",
    "should work with sourcemap generation",
    "should not remap against a stale sourcemap after a partial-file reload",
    "should work with sourcemap loading",
    "should work with sourcemap loading with large files"
  ],
  "test/cli/hot/watch-many-dirs.test.ts": [],
  "test/cli/hot/watch.test.ts": ["with ${watchedFile}"],
  "test/cli/init/init.test.ts": [
    "bun init works",
    "bun init with piped cli",
    "bun init in folder",
    "bun init error rather than overwriting file",
    "bun init utf-8",
    "bun init twice",
    "bun init --react works",
    "bun init --react=tailwind works",
    "bun init --react=shadcn works",
    "nested ",
    "bun init --minimal only creates package.json and tsconfig.json"
  ],
  "test/cli/inspect/BunFrontendDevServer.test.ts": [
    "should receive clientConnected and clientDisconnected events",
    "should notify on bundleStart and bundleComplete events",
    "should notify on bundleFailed events",
    "should notify on clientNavigated events",
    "sends exactly one clientNavigated event per SetUrl message",
    "should notify on consoleLog events",
    "[todo] should notify on clientErrorReported events",
    "[todo] should notify on graphUpdate events"
  ],
  "test/cli/inspect/HTTPServerAgent.test.ts": [
    "should receive serverRoutesUpdated event with route information"
  ],
  "test/cli/inspect/inspect.test.ts": [
    "bun ${args.join(",
    "bun --inspect=unix://",
    "bun --inspect=unix:",
    "BUN_INSPECT=",
    "fail",
    "success",
    "error.stack doesnt lose frames",
    "[todo] bun --inspect=ws+unix:///tmp/inspect.sock",
    "[todo] junit reporter"
  ],
  "test/cli/inspect/test-reporter.test.ts": [
    "retroactively reports tests when TestReporter.enable is called after tests are discovered",
    "test A1",
    "test A2",
    "test B1"
  ],
  "test/cli/install/GHSA-pfwx-36v6-832x.test.ts": [
    "should store integrity hash in lockfile for GitHub dependencies",
    "should verify integrity passes on re-install with matching hash",
    "should reject GitHub tarball when integrity check fails",
    "should update lockfile with integrity when old format has none",
    "should accept GitHub dependency from cache without re-downloading"
  ],
  "test/cli/install/architecture-match.test.ts": [
    "${arch} === true",
    "${arch} === false",
    "${os} === true",
    "${os} === false"
  ],
  "test/cli/install/bad-workspace.test.ts": [
    "bad workspace path",
    "workspace with ./ should not crash",
    "workspace with .\\\\ should not crash"
  ],
  "test/cli/install/bun-add.test.ts": [
    "should add existing package",
    "should reject missing package",
    "bun add --only-missing should not install existing package",
    "bun add --analyze should scan dependencies",
    "should reject positionals longer than 2048 bytes without stack overflow",
    "should handle semver-like names",
    "should handle @scoped names",
    "should add dependency with capital letters",
    "should add exact version with --exact",
    "should add to devDependencies with --dev",
    "should add to optionalDependencies with --optional",
    "should add to peerDependencies with --peer",
    "should add exact version with install.exact",
    "should add exact version with -E",
    "should add dependency with package.json in it and http tarball",
    "should add dependency with specified semver",
    "should add dependency (GitHub)",
    "should add dependency alongside workspaces",
    "should add aliased dependency (npm)",
    "should add aliased dependency (GitHub)",
    "git dep without package.json and with default branch",
    "should let you add the same package twice",
    "should install version tagged with ",
    "should handle Git URL in dependencies (SCP-style)",
    "should not save git urls twice",
    "should prefer optionalDependencies over dependencies of the same name",
    "should prefer dependencies over peerDependencies of the same name",
    "should add dependency without duplication",
    "should add dependency without duplication (GitHub)",
    "should add dependencies to workspaces directly",
    "should redirect ",
    "should add dependency alongside peerDependencies",
    "should add local tarball dependency",
    "should not add duplicate package.json entries when installing the same local folder twice (#30933)",
    "should not add duplicate package.json entries when installing the same tarball URL twice (#30499)",
    "should add multiple dependencies specified on command line",
    "should install tarball with tarball dependencies"
  ],
  "test/cli/install/bun-audit.test.ts": [],
  "test/cli/install/bun-create.test.ts": [
    "should create selected template with @ prefix",
    "should create selected template with @ prefix implicit ",
    "should create template from local folder",
    "should not mention cd prompt when created in current directory",
    "should create and install github template from ${repo}",
    "should not crash with --no-install and bun-create.postinstall starting with "
  ],
  "test/cli/install/bun-info.test.ts": [
    "should display package info for latest version",
    "should display package info for specific version",
    "should display specific property",
    "should handle missing arguments",
    "should display nested property",
    "should output JSON format with --json flag",
    "should handle non-existent package",
    "should handle non-existent version",
    "should handle non-existent property",
    "should handle malformed package specifier",
    "should handle scoped packages",
    "should handle .",
    "should handle version ranges",
    "versions should work",
    "versions[0] should work",
    "should handle dist-tags like latest"
  ],
  "test/cli/install/bun-install-cpu-os.test.ts": [
    "should filter dependencies by CPU architecture",
    "should filter dependencies by OS",
    "should filter dependencies by both CPU and OS",
    "should handle multiple CPU architectures in package metadata",
    "should error on invalid CPU architecture",
    "should error on invalid OS",
    "should skip installing packages with negated CPU/OS",
    "should support multiple CPU architectures",
    "should support multiple operating systems",
    "should support multiple CPU and OS combinations",
    "should support * wildcard for all architectures",
    "should support * wildcard for all operating systems",
    "should support negation with ! prefix"
  ],
  "test/cli/install/bun-install-lifecycle-scripts.test.ts": [
    "root package with all lifecycle scripts",
    "workspace lifecycle scripts",
    "dependency lifecycle scripts run before root lifecycle scripts",
    "install a dependency with lifecycle scripts, then add to trusted dependencies and install again",
    "adding a package without scripts to trustedDependencies",
    "lifecycle scripts run if node_modules is deleted",
    "INIT_CWD is set to the correct directory",
    "failing lifecycle script should print output",
    "failing root lifecycle script should print output correctly",
    "exit 0 in lifecycle scripts works",
    "--ignore-scripts should skip lifecycle scripts",
    "it should add ",
    "automatic node-gyp scripts should not run for untrusted dependencies, and should run after adding to ",
    "automatic node-gyp scripts work in package root",
    "auto node-gyp scripts work when scripts exists other than ",
    "does not add auto node-gyp script when ${script} script exists",
    "git dependencies also run ",
    "root lifecycle scripts should wait for dependency lifecycle scripts",
    "reach max concurrent scripts",
    "stress test",
    "it should install and use correct binary version",
    "node-gyp should always be available for lifecycle scripts",
    "default trusted dependencies should work",
    "bun pm ls --trusted uses default trusted list when trustedDependencies is not set",
    "default trusted dependencies should not be used of trustedDependencies is populated",
    "does not run any scripts if trustedDependencies is an empty list",
    "default trusted dependencies should only apply to npm packages, not file: dependencies",
    "file: dependency with default trusted name should run scripts when explicitly added to trustedDependencies",
    "trustedDependencies entry must match by name, not truncated hash",
    "will run default trustedDependencies after install that didn",
    "unhoisted untrusted scripts, none at root node_modules",
    "must not trust a truncated-hash collision in the trusted subtree",
    "initial install",
    "already installed",
    "existing trustedDependencies, unchanged trustedDependencies",
    "existing trustedDependencies, removing trustedDependencies",
    "non-existent trustedDependencies, then adding it",
    "node -p should work in postinstall scripts",
    "ensureTempNodeGypScript works",
    "bun pm trust and untrusted on missing package",
    "install",
    "without packages",
    "with a package"
  ],
  "test/cli/install/bun-install-native-binlink.test.ts": [
    "uses platform-specific bin instead of main package bin with linker ${linker}",
    "falls back to main package bin when platform dep has no matching bin file with linker ${linker}",
    "finds native bin at package root when parent bin path differs with linker ${linker}"
  ],
  "test/cli/install/bun-install-patch.test.ts": [
    "should patch a non-hoisted dependency",
    "should patch a transitive dependency",
    "should patch a transitive dependency after it was already installed",
    "should update a transitive dependency when the patchfile changes",
    "should update a scoped package",
    "shouldn",
    "should create patch for package and commit it",
    "should patch transitive dependency with isolated linker",
    "should handle scoped packages with isolated linker",
    "should work with workspaces and isolated linker",
    "should preserve patch after reinstall with isolated linker",
    "should handle multiple patches with isolated linker",
    "install with an empty cache downloads the package unpatched"
  ],
  "test/cli/install/bun-install-pathname-trailing-slash.test.ts": ["custom registry doesn"],
  "test/cli/install/bun-install-proxy.test.ts": ["bun install with proxy with big packages"],
  "test/cli/install/bun-install-registry.test.ts": [
    "symlinks (and junctions) are created correctly in the install cache",
    "valid --cafile",
    "valid --ca",
    "non-existent --cafile",
    "non-existent --cafile (absolute path)",
    "non-existent --cafile with workspaces exits 1 without crashing",
    "cafile from bunfig does not exist",
    "invalid cafile",
    "invalid --ca",
    "can get username",
    "username from .npmrc",
    "only .npmrc",
    "two .npmrc",
    "not logged in",
    "invalid token",
    "works for root and workspace packages",
    "install maintains indentation",
    "workspace sorting",
    "--frozen-lockfile",
    "resolvable lockfile with ${omit} dependencies disabled",
    "optionalPeers",
    "--lockfile-only",
    "(${textLockfile ? ",
    "exit code is ${optional ? 0 : 1} when ${optional ? ",
    "exit code is 0 when ${rootOptional ? ",
    "should not install optional deps if false in bunfig",
    "lifecycle scripts failures from transitive dependencies are ignored",
    "it should ignore peerDependencies within workspaces",
    "disabled dev/peer/optional dependencies are still included in the lockfile",
    "tarball override does not crash",
    "modifying package.json with --production should not save lockfile",
    "should prefer ${order[+prod % 2]} over ${order[1 - (+prod % 2)]}",
    "hardlinks on windows dont fail with long paths",
    "basic 1",
    "manifest cache will invalidate when registry changes",
    "dependency from root satisfies range from dependency",
    "duplicate names and versions in a manifest do not install incorrect packages",
    "replacing ${firstDep} with ${secondDep}",
    "optional",
    "peerDependency in child npm dependency should not maintain old version when package is upgraded",
    "package added after install",
    "--production excludes devDependencies in workspaces",
    "--production without a lockfile will install and not save lockfile",
    "existing non-symlink",
    "it should correctly link binaries after deleting node_modules",
    "will link binaries for packages installed multiple times",
    "it should re-symlink binaries that become invalid when updating package versions",
    "will only link global binaries for requested packages",
    "bin types${global ? ",
    "each type of binary serializes correctly to text lockfile",
    "root resolution bins",
    "it will skip (without errors) if a folder from ",
    "--config cli flag works",
    "it should invalid cached package if package.json is missing",
    "it should install with missing bun.lockb, node_modules, and/or cache",
    "it should hoist ${expected} when ${situation}",
    "hoisting/using incorrect peer dep after install",
    "root workspace (other than root) dependency will not hoist incorrect peer",
    "hoisting/using incorrect peer dep on initial install",
    "from normal dependency",
    "from workspace",
    "from linked package",
    "dependency with normal dependency same as root",
    "from hoisted workspace dependencies",
    "from non-hoisted workspace dependencies",
    "from root dependencies",
    "it should install folder dependencies with absolute paths",
    "name from manifest is scoped and url encoded",
    "duplicate peer dependency (one package is invalid_package_id)",
    "dist-tags",
    "exact versions stay exact",
    "without args",
    "update no args${latest ? ",
    "with package name in args",
    "update all",
    "update specific aliased package",
    "with pre and build tags",
    "--no-save will update packages in node_modules and not save to package.json",
    "update won",
    "update should update all packages in the current workspace",
    "update different dependency groups",
    "it should update packages from update requests",
    "--latest works with packages from arguments",
    "packages dependening on each other with aliases does not infinitely loop",
    "it should re-populate .bin folder if package is reinstalled",
    "one version with binary map",
    "multiple versions with binary map",
    "duplicate dependency in optionalDependencies maintains sort order",
    "missing package on reinstall, some with binaries",
    "--default",
    "no dependencies",
    "some dependencies, non with scripts",
    "it should be able to find binary in node_modules/.bin from parent directory of root package",
    "doesn",
    "dragon test 1",
    "dragon test 2",
    "dragon test 3",
    "dragon test 4",
    "dragon test 5",
    "dragon test 8",
    "dragon test 9",
    "dragon test 12",
    "it should not warn when the peer dependency resolution is compatible",
    "it should warn when the peer dependency resolution is incompatible",
    "it should install in such a way that two identical packages with different peer dependencies are different instances",
    "it should install in such a way that two identical packages with the same peer dependencies are the same instances (simple)",
    "it should install in such a way that two identical packages with the same peer dependencies are the same instances (complex)",
    "it shouldn",
    "it should reinstall and rebuild dependencies deleted by the user on the next install",
    "tarball ",
    "in workspace",
    "NO_COLOR works",
    "--filter with workspace names and paths",
    "dependency pattern args",
    "scoped workspace names",
    "catalog dependencies",
    "--recursive flag for outdated",
    "catalog grouping with multiple workspaces",
    "bun run ${bin} arg1 arg2",
    "bun --bun run ${bin} arg1 arg2",
    "bun --bun x ${bin} arg1 arg2",
    "${bin} arg1 arg2",
    "rejects dependency aliases containing relative path segments",
    "rejects package names containing relative path components in bun.lock",
    "rejects npm aliases whose manifest URL resolves to a different host than the registry",
    "registry override from a project .env only keeps the saved token when the host matches and the scheme is not downgraded",
    "[todo] text lockfile updates with new bin entry for folder dependencies",
    "[todo] only tagged versions in range errors",
    "[todo] dragon test 6",
    "[todo] dragon test 7",
    "[todo] dragon test 10"
  ],
  "test/cli/install/bun-install-retry.test.ts": ["retries on 500"],
  "test/cli/install/bun-install-security-provider.test.ts": [
    "basic",
    "shows progress message when scanner takes more than 1 second",
    "expect output to contain the advisory",
    "stdout contains all input package metadata",
    "scanner module not found",
    "scanner module throws during import",
    "scanner missing version field",
    "scanner wrong version",
    "scanner missing scan",
    "scanner scan not a function",
    "scanner returns non-array",
    "scanner returns null",
    "scanner returns undefined",
    "scanner throws exception",
    "scanner returns non-object in array",
    "advisory missing package field",
    "advisory package field not string",
    "advisory package field empty string",
    "advisory missing description field",
    "advisory with null description field",
    "advisory with empty string description",
    "advisory description field not string or null",
    "advisory missing url field",
    "advisory with null url field",
    "advisory with empty string url",
    "advisory missing level field",
    "advisory url field not string or null",
    "advisory invalid level",
    "advisory level not string",
    "second advisory invalid",
    "scanner process exits early",
    "scanner returns many advisories",
    "scanner with very large response",
    "multiple packages scanned",
    "advisory with both null description and url",
    "empty advisories array",
    "special characters in advisory",
    "unicode in advisory fields",
    "advisory without level field",
    "null values in level field",
    "scanner with version ranges",
    "scanner with latest tags",
    "handles packages JSON larger than max arg length (>1MB)"
  ],
  "test/cli/install/bun-install-stalled-tls.test.ts": [
    "bun install times out when the registry accepts TCP but never completes the TLS handshake"
  ],
  "test/cli/install/bun-install-streaming-extract.test.ts": [
    "tarballs below BUN_INSTALL_STREAMING_MIN_SIZE take the buffered path",
    "streaming rejects a tarball whose integrity does not match",
    "buffered extract: damaged-block retry resets header state (upstream semantics)",
    "buffered extract rejects a registry tarball whose decompressed size exceeds the limit"
  ],
  "test/cli/install/bun-install-tarball-integrity.test.ts": [
    "should store integrity hash for tarball URL in text lockfile",
    "should store integrity hash for local tarball in text lockfile",
    "should store consistent integrity hash for tarball URL across reinstalls",
    "should fail integrity check when tarball URL content changes",
    "should install successfully from text lockfile without integrity hash (backward compat)",
    "should add integrity hash to lockfile when re-resolving tarball dep",
    "should store consistent integrity hash for local tarball across reinstalls",
    "should produce same integrity hash for same tarball via URL and local path",
    "should install successfully from text lockfile without integrity hash for local tarball (backward compat)",
    "should fail (not hang) when tarball bytes don",
    "should fail (not hang) when registry returns 404 for tarball"
  ],
  "test/cli/install/bun-install.test.ts": [
    "bun install --network-concurrency=${input} fails",
    "bun install --network-concurrency=5 doesnt go over 5 concurrent requests",
    "should not error when package.json has comments and trailing commas",
    "when latest is ",
    "should report connection errors",
    "should support --registry CLI flag",
    "should work when moving workspace packages",
    "should work when renaming a single workspace package",
    "should handle missing package",
    "should handle @scoped authentication",
    "should send .npmrc _authToken on same-origin tarball download and withhold it cross-origin",
    "should handle empty string in dependencies",
    "should handle workspaces",
    "should handle ",
    "should handle workspaces with packages array",
    "should handle inter-dependency between workspaces",
    "should handle inter-dependency between workspaces (devDependencies)",
    "should handle inter-dependency between workspaces (optionalDependencies)",
    "should handle installing the same peerDependency with different versions",
    "should handle installing the same peerDependency with the same version",
    "should handle life-cycle scripts within workspaces",
    "should handle life-cycle scripts during re-installation",
    "should use updated life-cycle scripts in root during re-installation",
    "should use updated life-cycle scripts in dependency during re-installation",
    "should ignore workspaces within workspaces",
    "should handle ^0 in dependencies",
    "should handle ^1 in dependencies",
    "should handle ^0.0 in dependencies",
    "should handle ^0.1 in dependencies",
    "should handle ^0.0.0 in dependencies",
    "should handle ^0.0.2 in dependencies",
    "should handle matching workspaces from dependencies",
    "should edit package json correctly with git dependencies",
    "should handle ^0.0.2-rc in dependencies",
    "should handle ^0.0.2-alpha.3+b4d in dependencies",
    "should choose the right version with prereleases",
    "should handle ^0.0.2rc1 in dependencies",
    "should handle caret range in dependencies when the registry has prereleased packages, issue#4398",
    "should prefer latest-tagged dependency",
    "should install latest with prereleases",
    "should handle dependency aliasing",
    "should handle dependency aliasing (versioned)",
    "should handle dependency aliasing (dist-tagged)",
    "should not reinstall aliased dependencies",
    "should handle aliased & direct dependency references",
    "should not hoist if name collides with alias",
    "should get npm alias with matching version",
    "should not apply overrides to package name of aliased package",
    "should handle unscoped alias on scoped dependency",
    "should handle scoped alias on unscoped dependency",
    "should handle aliased dependency with existing lockfile",
    "should handle GitHub URL in dependencies (user/repo)",
    "should handle GitHub URL in dependencies (user/repo#commit-id)",
    "should handle GitHub URL in dependencies (user/repo#tag)",
    "install ${dep}",
    "add ${dep}",
    "should handle GitHub URL in dependencies (github:user/repo#tag)",
    "should handle GitHub URL in dependencies (https://github.com/user/repo.git)",
    "should handle GitHub URL in dependencies (git://github.com/user/repo.git#commit)",
    "should handle GitHub URL in dependencies (git+https://github.com/user/repo.git)",
    "should handle GitHub tarball URL in dependencies (https://github.com/user/repo/tarball/ref)",
    "should handle GitHub tarball URL in dependencies (https://github.com/user/repo/tarball/ref) with custom GITHUB_API_URL",
    "should treat non-GitHub http(s) URLs as tarballs (https://some.url/path?stuff)",
    "should handle GitHub URL with existing lockfile",
    "should consider peerDependencies during hoisting",
    "should install peerDependencies when needed",
    "should not regard peerDependencies declarations as duplicates",
    "should report error on invalid format for optionalDependencies",
    "should report error on duplicated workspace packages",
    "should handle Git URL in dependencies",
    "should handle Git URL in dependencies (SCP-style)",
    "should handle Git URL with committish in dependencies",
    "should fail on invalid Git URL",
    "should fail on ssh Git URL if invalid credentials",
    "should fail on Git URL with invalid committish",
    "should de-duplicate committish in Git URLs",
    "should handle Git URL with existing lockfile",
    "should prefer optionalDependencies over dependencies of the same name",
    "should prefer dependencies over peerDependencies of the same name",
    "should handle tarball URL",
    "should handle tarball path",
    "should handle tarball URL with aliasing",
    "should handle tarball path with aliasing",
    "should de-duplicate dependencies alongside tarball URL",
    "should handle tarball URL with existing lockfile",
    "should handle tarball path with existing lockfile",
    "should handle devDependencies from folder",
    "should deduplicate devDependencies from folder",
    "should install dependencies in root package of workspace",
    "should install dependencies in root package of workspace (*)",
    "should ignore invalid workspaces from parent directory",
    "should handle --cwd",
    "should handle --frozen-lockfile",
    "should handle bun ci alias (to --frozen-lockfile)",
    "should handle frozenLockfile in config file",
    "should perform bin-linking across multiple dependencies",
    "should handle trustedDependencies",
    "should handle installing packages from inside a workspace with ",
    "should handle installing packages from inside a workspace without prefix",
    "should handle installing workspaces with more complicated globs",
    "should handle installing workspaces with multiple glob patterns",
    "should handle installing packages inside workspaces with difference versions",
    "should override npm dependency by matching workspace",
    "should not override npm dependency by workspace with mismatched version",
    "should override @scoped npm dependency by matching workspace",
    "should override aliased npm dependency by matching workspace",
    "should override child npm dependency by matching workspace",
    "should not override child npm dependency by workspace with mismatched version",
    "should override @scoped child npm dependency by matching workspace",
    "should override aliased child npm dependency by matching workspace",
    "should install peer dependencies from root package",
    "should install correct version of peer dependency from root package",
    "should ${fails ? ",
    "shouldn",
    "should ensure read permissions of all extracted files",
    "should handle @scoped name that contains tilde, issue#7045",
    "should read install.saveTextLockfile from bunfig.toml",
    "providing invalid url in lockfile does not crash",
    "optional dependencies do not need to be resolvable in text lockfile",
    "non-optional dependencies need to be resolvable in text lockfile",
    "rejects dependency aliases containing ",
    "does not extract a tarball for a dependency alias containing ",
    "does not install transitive file: dependencies that point outside their package",
    "does not install transitive file: dependencies with overlong folder targets",
    "installs a file: dependency pointing outside the project when it came from root package.json ",
    "still rejects transitive file: dependencies that escape their package when a different name is in ",
    "does not extract a local file: tarball outside the temp dir for a dependency alias containing ",
    "does not create a cache index entry outside the cache directory for a dependency alias of ",
    "[todo] should handle installing workspaces with absolute glob patterns",
    "[todo] shouldn"
  ],
  "test/cli/install/bun-link.test.ts": [
    "should link and unlink workspace package",
    "should link package",
    "should link scoped package",
    "should link dependency without crashing"
  ],
  "test/cli/install/bun-lock.test.ts": [
    "should write plaintext lockfiles",
    "should be the default save format",
    "should save the lockfile if --save-text-lockfile and --frozen-lockfile are used",
    "should convert a binary lockfile with invalid optional peers",
    "should not deduplicate bundled packages with un-bundled packages",
    "should not change formatting unexpectedly",
    "should sort overrides before comparing",
    "should include unused resolutions in the lockfile",
    "requires an integrity hash for an off-registry npm tarball URL at lockfileVersion 2",
    "escapes double quotes in npm registry tarball URLs when saving bun.lock",
    "escapes quotes and newlines in requested version literals when writing yarn.lock",
    "prints an actionable error for a lockfile version newer than this build supports"
  ],
  "test/cli/install/bun-lockb.test.ts": [
    "should not print anything to stderr when running bun.lockb",
    "should continue using a binary lockfile if it exists",
    "recovers from a corrupted binary lockfile instead of panicking",
    "rejects a binary lockfile whose patched-dependency flag byte is out of range",
    "rejects a binary lockfile whose git resolved tag contains path separators"
  ],
  "test/cli/install/bun-pack.test.ts": [
    "basic",
    "in subdirectory",
    "rejects name and version containing parent directory components",
    "missing",
    "scoped name: ${input}",
    "--dry-run",
    "--gzip",
    "--destination=",
    "--filename=",
    "--filename and --destination",
    "--ignore-scripts",
    "--quiet",
    "shasum and integrity are consistent",
    "in a workspace",
    "in a workspace subdirectory",
    "replaces workspace: protocol without lockfile",
    "replaces workspace: protocol with lockfile: ${input}",
    "fails gracefully when workspace version fails to resolve",
    "lifecycle scripts execution order",
    "lifecycle script modifying version updates tarball filename (#17195)",
    "basic (${bundledDependencies})",
    "basic (bundledDependencies: true)",
    "scoped bundledDependencies",
    "invalid bundledDependencies value should throw",
    "resolve dep of bundled dep",
    "scoped names",
    "ignore deps that aren",
    "CHANGELOG is not included by default",
    ".npmignore cannot exclude CHANGELOG",
    ".npmignore cannot exclude LICENSE",
    "can include files and directories",
    "matches relative to root by default",
    "matches ",
    "recursive only if leading **/",
    "excluded entries within included directories are not included",
    "foo",
    "can ignore and un-ignore a file (${ignoreFile})",
    "excludes files recursively",
    "directory",
    "are included even if not included in ",
    "deduplicate with ",
    "unicode",
    "$npm_command is accurate",
    "$npm_lifecycle_event is accurate"
  ],
  "test/cli/install/bun-patch.test.ts": [
    "${pkgName}@${version}",
    "should patch a package when it is already patched",
    "bad patch arg",
    "bad patch commit arg",
    "overwriting module with multiple levels of directories",
    "should overwrite the node_modules folder of the package",
    "should overwrite nested node_modules folder of the package"
  ],
  "test/cli/install/bun-pm-pkg.test.ts": [
    "should get a single property",
    "should get multiple properties",
    "should get entire package.json when no args provided",
    "should get nested properties with dot notation",
    "should get array elements with bracket notation",
    "should get object properties with bracket notation",
    "should get array elements with dot notation (npm compatibility)",
    "should get array elements with dot numeric index",
    "should get array elements without index (entire array)",
    "should handle missing properties gracefully",
    "should handle mixed existing and missing properties",
    "should handle boolean values",
    "should handle number values",
    "should handle null values",
    "should handle numeric property names on objects",
    "should fail gracefully when no package.json found",
    "should set a simple string property",
    "should set multiple properties",
    "should set nested properties with dot notation",
    "should create nested objects when they don",
    "should handle JSON boolean true with --json flag",
    "should handle JSON boolean false with --json flag",
    "should handle JSON null with --json flag",
    "should handle JSON integers with --json flag",
    "should handle JSON floats with --json flag",
    "should handle JSON objects with --json flag",
    "should handle JSON arrays with --json flag",
    "should treat values as strings without --json flag",
    "should preserve file formatting",
    "should fail with invalid key=value format",
    "should fail with empty key",
    "should fail when no arguments provided",
    "should delete a property",
    "should delete nested properties",
    "should handle deleting non-existent properties",
    "should delete multiple properties",
    "should show help",
    "should show help when no subcommand provided",
    "should show help for unknown subcommand",
    "should handle malformed JSON gracefully",
    "should handle non-object root gracefully",
    "should handle very deeply nested properties",
    "should maintain npm pkg compatibility",
    "should work in workspace root",
    "should work in workspace package directory",
    "should modify workspace package.json without affecting root",
    "should modify root without affecting workspace packages",
    "should find package.json in deeply nested directories",
    "should find nearest package.json in nested structure",
    "should handle modifications from deeply nested directories",
    "should handle all data types correctly",
    "should handle complex nested structures",
    "should produce equivalent output to npm pkg for common operations",
    "should handle mixed bracket and dot notation equivalently",
    "should handle complex mixed notation patterns",
    "should handle string properties in bracket notation",
    "should handle numeric indices with different data types",
    "should gracefully handle invalid notation patterns",
    "should reject empty bracket notation for get operations (npm compatibility)",
    "should maintain consistency between set and get operations",
    "should handle edge cases with special characters",
    "should verify npm compatibility with real-world patterns",
    "should fix uppercase package names to lowercase",
    "should warn about missing bin files",
    "should not modify package.json if no fixes are needed",
    "should handle package.json with existing bin files",
    "should preserve all other package.json fields",
    "should handle malformed package.json gracefully",
    "should handle non-object package.json",
    "should fix multiple issues in one run",
    "should not crash on empty bin object",
    "should handle missing package.json file",
    "should get empty string property key (npm compatibility)",
    "should set empty string property key",
    "[todo] should delete empty string property key"
  ],
  "test/cli/install/bun-pm-scan.test.ts": [
    "shows error when no security scanner configured",
    "shows error when lockfile doesn",
    "shows error when package.json doesn",
    "scanner receives correct package format",
    "scanner version validation",
    "detects fatal vulnerabilities",
    "detects warning vulnerabilities",
    "handles mixed vulnerabilities",
    "no vulnerabilities found",
    "shows correct path for direct dependencies",
    "shows correct path for transitive dependencies",
    "handles scanner crash",
    "handles invalid JSON from scanner",
    "handles missing required fields in advisory",
    "singular vs plural in summary",
    "shows timing for slow scans",
    "does not show "
  ],
  "test/cli/install/bun-pm-version.test.ts": [
    "should show help when no arguments provided",
    "shows help with version previews",
    "should increment versions correctly",
    "should set specific version",
    "handles empty package.json",
    "handles various error conditions",
    "handles missing package.json like npm",
    "handles empty string package.json like npm",
    "handles malformed JSON like npm",
    "creates git commits and tags by default",
    "supports custom commit messages",
    "fails when git working directory is not clean",
    "allows dirty working directory with --force flag",
    "works without git when no repo is present",
    "respects --no-git-tag-version flag",
    "respects --git-tag-version=false flag",
    "respects --git-tag-version=true flag",
    "supports %s substitution in commit messages",
    "preserves JSON formatting correctly",
    "handles custom preid and prerelease scenarios",
    "should preserve prerelease identifiers correctly",
    "runs lifecycle scripts in correct order and handles failures",
    "should version workspace packages individually",
    "should work from subdirectories"
  ],
  "test/cli/install/bun-pm-why.test.ts": [
    "should show help when no package is specified",
    "should show direct dependency",
    "should show nested dependencies",
    "should handle workspace dependencies",
    "should handle npm aliases",
    "should show error for non-existent package",
    "should show dependency types correctly",
    "should handle packages with multiple versions",
    "should handle deeply nested dependencies",
    "should support glob patterns for package names",
    "should support version constraints in the query",
    "should handle nested workspaces",
    "should support the --top flag to limit dependency tree depth",
    "should support the --depth flag to limit dependency tree depth"
  ],
  "test/cli/install/bun-pm.test.ts": [
    "should list top-level dependency",
    "should list all dependencies",
    "should list top-level aliased dependency",
    "should list aliased dependencies",
    "should list only trusted dependencies with --trusted",
    "should list only trusted dependencies with --all --trusted",
    "should list trusted transitive dependencies under untrusted parents with --all --trusted (isolated)",
    "should list nothing with --trusted when no dependencies are trusted",
    "should remove all cache",
    "bun pm migrate",
    "bun whoami executes pm whoami",
    "bun pm whoami still works",
    "bun list --all shows full dependency tree",
    "bun pm cache rm resolves the cache directory from the process environment, ignoring project-local .env overrides",
    "bun pm cache rm does not create the directory named by a project-local .env override"
  ],
  "test/cli/install/bun-publish.test.ts": [
    "mock web login",
    "otp failure",
    "npm-notice with login url${shouldIgnoreNotice ? ",
    "CI user agent name: ${envInfo.ci}",
    "can publish a package then install it",
    "can publish from a tarball",
    "can publish scoped packages",
    "can publish and install binaries with ${JSON.stringify(info)}",
    "dependencies are installed",
    "can publish workspace package",
    "does not publish",
    "does not publish from tarball path",
    "should run in order${arg.length > 0 ? ",
    "--ignore-scripts",
    "prepublishOnly modifying version publishes correct version (#17195)",
    "attempting to publish a private package should fail",
    "--access",
    "access ${access}",
    "can publish with a tag",
    "$npm_command is accurate during publish",
    "$npm_lifecycle_event is accurate during publish",
    "workspace publish sends README contents as readme / readmeFilename",
    "tarball publish sends README contents from inside the archive",
    "republishing normally fails",
    "republishing with --tolerate-republish skips when version exists",
    "republishing tarball with --tolerate-republish skips when version exists"
  ],
  "test/cli/install/bun-remove.test.ts": [],
  "test/cli/install/bun-run-bunfig.test.ts": [
    "which node",
    "command not found",
    "autoload local bunfig.toml (same cwd)",
    "NOT autoload local bunfig.toml (sub cwd)",
    "NOT autoload home bunfig.toml"
  ],
  "test/cli/install/bun-run-dir.test.ts": [],
  "test/cli/install/bun-run.test.ts": [
    "respecting ",
    "falling back to index",
    "invalid tsconfig.json is ignored",
    "--silent omits error messages",
    "no --silent includes error messages",
    "valid tsconfig.json with invalid extends doesn",
    "falling back to index with no package.json",
    "should not passthrough script arguments to pre- or post- scripts",
    "should show the correct working directory when run with --cwd",
    "DCE annotations are respected",
    "--ignore-dce-annotations ignores DCE annotations",
    "$npm_command is accurate",
    "$npm_lifecycle_event is accurate",
    "$npm_package_config_* works",
    "does not crash after spawning with $ variable",
    "should pass arguments correctly in scripts",
    "bun ",
    "should run from stdin",
    "outside",
    "inside",
    "inside --shell=bun",
    "inside script",
    "inside wrong script",
    "outside 2",
    "inside 2",
    "should run main from monorepo root",
    "should run package_a from package_a",
    "should fail from package_b"
  ],
  "test/cli/install/bun-security-scanner-matrix-with-node-modules.test.ts": [],
  "test/cli/install/bun-security-scanner-matrix-without-node-modules.test.ts": [],
  "test/cli/install/bun-security-scanner-workspaces.test.ts": [
    "security scanner receives packages from workspace dependencies",
    "security scanner receives packages from workspace dependencies with hoisted linker",
    "security scanner receives packages from workspace dependencies with isolated linker"
  ],
  "test/cli/install/bun-update-security-edge-cases.test.ts": [
    "bun update detects vulnerability in updated version that was safe before",
    "bun update <pkg> detects vulnerability in the specific updated package",
    "bun update detects newly discovered vulnerability in existing package",
    "bun pm scan detects vulnerability in existing transitive dependency after adding package",
    "bun update with version range change exposes vulnerability",
    "bun pm scan detects newly discovered vulnerabilities in existing lockfile"
  ],
  "test/cli/install/bun-update-security-provider.test.ts": [
    "security scanner blocks bun update on fatal advisory",
    "security scanner does not run on bun update when disabled"
  ],
  "test/cli/install/bun-update-security-scan-all.test.ts": [
    "bun update without arguments scans all packages",
    "bun update with specific packages only scans those packages",
    "bun update respects security scanner configuration",
    "bun update aborts on fatal vulnerabilities",
    "bun update shows dependency paths correctly",
    "[todo] bun update prompts for warnings when TTY available - requires TTY for interactive prompt"
  ],
  "test/cli/install/bun-update-security-simple.test.ts": [
    "security scanner blocks bun update with fatal advisory",
    "security scanner does not run on bun update when not configured"
  ],
  "test/cli/install/bun-update.test.ts": [
    "should update to latest version of dependency (${input.baz[0]})",
    "should update to latest versions of dependencies (${input.baz[0]})",
    "lockfile should not be modified when there are no version changes, issue#5888",
    "should support catalog versions in update",
    "should support --recursive flag",
    "should print UTF-8 arrows correctly with colors enabled"
  ],
  "test/cli/install/bun-upgrade.test.ts": [
    "two invalid arguments, should display error message and suggest command",
    "two invalid arguments flipped, should display error message and suggest command",
    "one invalid argument, should display error message and suggest command",
    "one valid argument, should succeed",
    "two valid argument, should succeed",
    "zero arguments, should succeed",
    "recreates the staging directory in the temp dir instead of reusing a pre-existing one"
  ],
  "test/cli/install/bun-workspaces.test.ts": [
    "dependency on workspace without version in package.json",
    "allowing negative workspace patterns",
    "dependency on same name as workspace and dist-tag",
    "successfully installs workspace when path already exists in node_modules",
    "adding workspace in workspace edits package.json with correct version (workspace:*)",
    "workspaces with invalid versions should still install",
    "combination",
    "version range ${version} and workspace with no version",
    "version range ${version} and workspace with no version (should fail)",
    "does not crash when root package.json is in ",
    "cwd in workspace script is not the symlink path on windows",
    "from package.json",
    "from cli",
    "many concurrent local tarballs in workspaces",
    "$npm_package_config_ works in root",
    "$npm_package_config_ works in root in subpackage",
    "adding packages in a subdirectory of a workspace",
    "adding packages in workspaces",
    "it should detect duplicate workspace dependencies",
    "it should allow duplicates, root@${rootVersion}, package@${packageVersion}",
    "it should allow listing workspace as dependency of the root package version ${version}",
    "does not run root scripts if root is filtered out",
    "basic",
    "all but one or two",
    "matched workspace depends on filtered workspace",
    "filter with a path",
    "can override npm package with workspace package under a different name",
    "linkWorkspacePackages = false uses registry instead of linking workspace packages",
    "linkWorkspacePackages = false but workspace: prefix still links workspace",
    "matching workspace devDependency and npm peerDependency"
  ],
  "test/cli/install/bunx.test.ts": [
    "should error when --package is provided without package name",
    "should error when --package is provided without binary name",
    "should install specified package when binary differs from package name",
    "should support -p shorthand with mock registry",
    "should support --package=<pkg> syntax with mock registry",
    "should fail to run alternate binary without --package flag",
    "should execute the correct binary when package has multiple binaries",
    "locally installed ",
    "bunx-cache-only "
  ],
  "test/cli/install/catalogs.test.ts": [
    "both catalog and catalogs ${isTopLevel ? ",
    "detect changes (${binaryLockfile ? ",
    "fails gracefully when no catalog is found for a package",
    "invalid dependency version"
  ],
  "test/cli/install/config-version.test.ts": [
    "should add configVersion@v0 to an existing lockfile"
  ],
  "test/cli/install/hoist.test.ts": [
    "should handle resolving optional peer from multiple instances of same package"
  ],
  "test/cli/install/hosted-git-info/boundary-conditions.test.ts": [],
  "test/cli/install/hosted-git-info/from-url.test.ts": [],
  "test/cli/install/hosted-git-info/parse-url.test.ts": [],
  "test/cli/install/install-fd-leak.test.ts": [],
  "test/cli/install/isolated-install.test.ts": [
    "single dependency",
    "scope package",
    "transitive dependencies",
    "handles cyclic dependencies",
    "package with dependency on previous self works",
    "can install folder dependencies",
    "can install folder dependencies on root package",
    "basic",
    "workspace self dependencies create symlinks",
    "will resolve if available through another importer (${name})",
    "successfully resolves optional peer with nested package",
    "isolated install with backend: ${backend}",
    "root and workspace node_modules are reset",
    "some workspaces don",
    "can override linker from bunfig",
    "works as the only config option",
    "many transitive dependencies",
    "dependency names are preserved",
    "same resolution, different dependency name",
    "successfully removes and corrects symlinks",
    "runs lifecycle scripts correctly",
    "transitive peer deps are resolved when resolution is fully synchronous",
    "is disabled by default",
    "can be enabled via BUN_INSTALL_GLOBAL_STORE=1",
    "can be enabled via bunfig install.globalStore",
    "survives node_modules wipe",
    "--force replaces a corrupted global-store entry",
    "BUN_INSTALL_GLOBAL_STORE=0 overrides bunfig globalStore = true",
    "entry hash is deterministic across fresh installs",
    "different resolved transitive dep produces a different entry hash",
    "two projects with the same closure share one global entry",
    "workspace dependency makes the parent entry project-local",
    "packages with trusted lifecycle scripts stay project-local",
    "concurrent installs into a cold global store both succeed",
    "a leftover staging directory does not shadow the published entry",
    "bun",
    "an entry that loses global-store eligibility detaches without mutating the shared entry",
    "upgrades a pre-global-store node_modules in place",
    "preserves bun patch workspace when install runs before --commit",
    "rejects dependency aliases that traverse outside node_modules",
    "invalid --linker value is echoed back in the error"
  ],
  "test/cli/install/lockfile-only.test.ts": [],
  "test/cli/install/lockfile-version-2.test.ts": [
    "a freshly written text lockfile defaults to version 2",
    "re-saving a v1 lockfile keeps it at version 1 even after adding a dependency",
    "re-saving a v0 lockfile floors it to version 1 so it stays parseable",
    "an existing v1 lockfile still loads (backward compatible)",
    "off-registry npm tarball integrity is enforced only at version 2",
    "unsafe git .bun-tag is rejected only at version 2",
    "unsafe github .bun-tag is rejected at every version",
    "re-saving a v1 off-registry lockfile keeps it at version 1",
    "re-saving keeps v1 for a tarball under a writer-only scoped registry"
  ],
  "test/cli/install/migrate-bun-lockb-v2.test.ts": ["migrate ${testInfo.name}"],
  "test/cli/install/migration/complex-workspace.test.ts": [
    "${packageName} is ${realPackageName ? ",
    "${filePath} exists",
    "${filePath} does not exist",
    "the install succeeds"
  ],
  "test/cli/install/migration/migrate.test.ts": [
    "migrate from npm during ",
    "migrate from npm lockfile v2 during ",
    "migrate package with dependency on root package",
    "migrate package with npm dependency that resolves to a git package",
    "migrate from npm lockfile that is missing ",
    "npm lockfile with relative workspaces",
    "should create bun.lock if ${lockfile} migration fails",
    "npm lockfile migration skips extraneous packages that also declare inBundle: false",
    "package-lock.json migration requires integrity for tarball URLs outside the configured registry",
    "package-lock.json migration rejects git committish values that are not a single path component",
    "package-lock.json migration keeps dependencies declared as arbitrary tarball URLs without integrity",
    "[todo] migrate workspace from npm during "
  ],
  "test/cli/install/migration/pnpm-comprehensive.test.ts": [
    "large single package with many dependencies",
    "complex monorepo with cross-dependencies",
    "pnpm with patches and overrides",
    "pnpm v6 format (unsupported)",
    "pnpm with peer dependencies and auto-install-peers"
  ],
  "test/cli/install/migration/pnpm-lock-migration.test.ts": [
    "simple pnpm lockfile migration produces correct bun.lock",
    "pnpm workspace lockfile migration",
    "pnpm with npm protocol aliases",
    "handles different pnpm lockfile versions",
    "handles missing pnpm-lock.yaml gracefully"
  ],
  "test/cli/install/migration/pnpm-migration-complete.test.ts": [
    "comprehensive PNPM migration with all edge cases"
  ],
  "test/cli/install/migration/pnpm-migration.test.ts": [
    "basic",
    "version is number with dot",
    "manifests are fetched for bins",
    "peers basic",
    "workspaces with peers",
    "patches are detected and migrated correctly",
    "links to the root package are resolved correctly",
    "accross workspaces",
    "workspace dependencies",
    "catalogs, peers, and workspaces",
    "[todo] basic",
    "[todo] from npm"
  ],
  "test/cli/install/migration/yarn-lock-migration.test.ts": [
    "simple yarn.lock migration produces correct bun.lock",
    "yarn.lock with packages containing long build tags",
    "yarn.lock with extremely long build tags (regression test)",
    "complex yarn.lock with multiple dependencies and versions",
    "yarn.lock with npm aliases",
    "yarn.lock with resolutions",
    "yarn.lock with workspace dependencies",
    "yarn.lock with scoped packages and parent/child relationships",
    "migration with realistic complex yarn.lock",
    "yarn.lock with packages that have os/cpu requirements"
  ],
  "test/cli/install/minimum-release-age.test.ts": [
    "filters packages by minimum release age",
    "respects float values for days",
    "handles exact version requests",
    "detects rapid bugfixes and selects stable version",
    "detects rapid bugfixes with dist-tag (latest)",
    "gives up after searching 7 days beyond age gate",
    "filters canary versions correctly",
    "compares only base prerelease tag (canary vs beta)",
    "handles latest with prerelease dist-tag",
    "stops searching after 7 days beyond minimum age",
    "with daily releases, gets the latest within minimum age (not searching beyond 7 days)",
    "version range finds old stable version beyond search window",
    "excludes packages from filtering via bunfig",
    "bunfig.toml configuration works",
    "CLI flag overrides bunfig.toml",
    "handles 0 value to disable",
    "global bunfig.toml configuration works",
    "local bunfig overrides global bunfig",
    "shows filtering decisions in verbose mode",
    "warnings only for direct dependencies",
    "handles empty version lists",
    "cache version includes timestamp data",
    "timestamps computed once per operation",
    "works with --dry-run",
    "works with bun update",
    "works with bun outdated",
    "transitive dependencies are not filtered by minimum-release-age",
    "git dependencies are not affected by minimum-release-age",
    "ignores file dependencies",
    "handles scoped packages",
    "frozen lockfile preserves existing versions regardless of minimum-release-age",
    "works with frozen lockfile when versions are old enough",
    "rejects negative minimum-release-age",
    "rejects non-numeric minimum-release-age",
    "handles package with missing time field",
    "handles invalid timestamp formats",
    "handles package released exactly at minimum age threshold",
    "filters devDependencies with minimum-release-age",
    "filters optionalDependencies with minimum-release-age",
    "filters mixed dependency types",
    "handles packages with future timestamps",
    "handles all versions with future timestamps",
    "only passes age-filtered packages to security scanner",
    "scanner receives correct version when stability check downgrades",
    "scanner can report advisory on age-filtered package",
    "excludes filter bypasses age check but scanner still sees package"
  ],
  "test/cli/install/npmrc.test.ts": [
    "should convert to utf8 if BOM",
    "works with empty file",
    "sets default registry",
    "sets scoped registry",
    "works with home config",
    "works with two configs",
    "package config overrides home config",
    "default registry from env variable",
    "default registry from env variable 2",
    "authentication works",
    "sets scoped registry option: ${name}",
    "applies auth tokens to default registry correctly - same host different paths",
    "auth token not applied when paths don"
  ],
  "test/cli/install/overrides.test.ts": [
    "overrides affect your own packages",
    "overrides affects all dependencies",
    "overrides being set later affects all dependencies",
    "overrides to npm specifier",
    "changing overrides makes the lockfile changed, prevent frozen install",
    "overrides reset when removed",
    "overrides do not apply to workspaces"
  ],
  "test/cli/install/public-hoist-pattern.test.ts": [
    "bunfig string",
    "bunfig array",
    "all exclude pattern",
    "all include pattern",
    "mixed include and exclude patterns",
    "npmrc string configuration",
    "npmrc array configuration",
    "npmrc mixed patterns",
    "exclude specific packages",
    "scoped package patterns",
    "complex pattern combinations",
    "workspaces with publicHoistPattern",
    "invalid publicHoistPattern type in bunfig",
    "malformed bunfig with array syntax"
  ],
  "test/cli/install/redacted-config-logs.test.ts": [],
  "test/cli/install/semver.test.ts": [
    "whitespace bug fix",
    "comparisons",
    "equality",
    "expected errors",
    "failures does not cause weird memory issues",
    "exact versions",
    "ranges",
    "range includes",
    "range excludes",
    "pre-release snapshot",
    "a version range with >=256 || comparators does not abort",
    "a range with a dangling ",
    "a version range with hundreds of thousands of "
  ],
  "test/cli/install/shebang-normalize.test.ts": [],
  "test/cli/install/symlink-path-traversal.test.ts": [
    "rejects symlink targets that climb above the package root before re-entering a ",
    "should skip symlinks with relative path traversal targets",
    "should skip symlinks with absolute path targets",
    "should allow safe relative symlinks within the package (install succeeds)"
  ],
  "test/cli/install/test-dev-peer-dependency-priority.test.ts": [
    "workspace devDependencies should take priority over peerDependencies for resolution",
    "devDependencies and peerDependencies with different versions should coexist",
    "dependency behavior comparison prioritizes devDependencies",
    "Next.js monorepo scenario should not make unnecessary network requests"
  ],
  "test/cli/run/as-node.test.ts": [
    "the node cli actually works",
    "doesnt resolve bins",
    "doesnt resolve scripts",
    "can run a script named run.js",
    "picks tsx over any other ext",
    "picks jsx over ts",
    "picks mts over ts",
    "picks ts over js/cjs/etc",
    "node -e ",
    "process args work",
    "no args is exit code zero for now"
  ],
  "test/cli/run/autoinstall-cached-manifest.test.ts": [
    "auto-install with cached manifest but missing tarball does not read a dangling dependency pointer"
  ],
  "test/cli/run/commonjs-invalid.test.ts": ["Loading an invalid commonjs module"],
  "test/cli/run/commonjs-no-export.test.ts": ["CommonJS entry point with no exports"],
  "test/cli/run/cpu-prof.test.ts": [
    "generates CPU profile with default name",
    "--cpu-prof-name sets custom filename",
    "--cpu-prof-dir sets custom directory",
    "profile captures function names",
    "--cpu-prof-md generates markdown format profile",
    "--cpu-prof-md with custom name",
    "--cpu-prof-md shows function details with relationships",
    "--cpu-prof-md works standalone without --cpu-prof",
    "--cpu-prof and --cpu-prof-md together creates both files"
  ],
  "test/cli/run/empty-file.test.ts": ["should execute empty scripts"],
  "test/cli/run/env.test.ts": [
    ".env",
    ".env.local",
    ".env.development (NODE_ENV=undefined)",
    ".env.development (NODE_ENV=development)",
    ".env.production",
    ".env.development and .env.test ignored when NODE_ENV=production",
    ".env.production and .env.test ignored when NODE_ENV=development",
    ".env and .env.test used in testing",
    ".env.local ignored when bun test",
    ".env.development and .env.production ignored when bun test",
    "NODE_ENV is automatically set to test within bun test",
    "process env overrides everything else",
    ".env.{NODE_ENV}.local overrides .env.local",
    ".env.local overrides .env.{NODE_ENV}",
    ".env.{NODE_ENV} overrides .env",
    ".env colon assign",
    ".env export assign",
    ".env value expansion",
    ".env comments",
    ".env process variables no comments",
    "NODE_ENV=production",
    "NODE_ENV=development",
    ".env escaped dollar sign",
    ".env doesnt crash with 159 bytes",
    ".env with 50000 entries",
    ".env space edgecase (issue #411)",
    ".env special characters 1 (issue #2823)",
    "env escaped quote (issue #2484)",
    ".env Windows-style newline (issue #3042)",
    ".env with zero length strings",
    "process with zero length environment variable",
    ".env in a folder doesn",
    "#3911",
    "buffer boundary",
    "only Bun.env",
    "only process.env",
    "only import.meta.env",
    "import.meta.env as 1st access",
    "import.meta.env as 2nd access",
    "single arg",
    "multiple args",
    "single arg with multiple files",
    "priority on multi-file single arg",
    "priority on multiple args",
    "priority on process env",
    "absolute filepath",
    "explicit relative filepath",
    "subdirectory filepath",
    "when arg missing, fallback to default dotenv behavior",
    "empty string disables default dotenv behavior",
    "should correctly ignore invalid values and parse the rest",
    "should ignore a file that doesn",
    "basic case",
    "pass explicit NODE_ENV case",
    "pass weird NODE_ENV case",
    "in bun test",
    "my test",
    "in bun test with explicit setting",
    "in bun test with dynamic access",
    "in bun test with dynamic access + explicit set",
    "NODE_ENV has a default value",
    "NODE_ENV default is not propogated in bun run",
    "does not pass variables from .env files into scripts",
    "explicit NODE_ENV=",
    "NODE_ENV=test loads .env.test even when .env.production exists",
    "handles large quoted values with escape sequences",
    "handles multiple large values in same file",
    "handles escape sequences at buffer boundaries",
    "[todo] src boundary"
  ],
  "test/cli/run/esm-defineProperty.test.ts": [
    "defineProperty",
    "shows __esModule if it was exported",
    "arraylike"
  ],
  "test/cli/run/filter-workspace.test.ts": [
    "resolve ${name} from ${d}",
    "resolve ",
    "resolve all from ${d}",
    "works with auto command",
    "resolve all with glob",
    "resolve all with recursive glob",
    "resolve package with missing name",
    "run in parallel",
    "run pre and post scripts, in order",
    "respect dependency order",
    "respect dependency order when dependency name is larger than 8 characters",
    "ignore dependency order on cycle, preserving pre and post script order",
    "detect cycle of length > 2",
    "should error with missing script",
    "should warn about malformed package.json",
    "nonzero exit code on failure",
    "elides output by default when using --filter",
    "respects --elide-lines argument",
    "--elide-lines=0 shows all output",
    "--elide-lines is a no-op (not an error) when stdout is not a terminal",
    "warning names which package.json failed to parse"
  ],
  "test/cli/run/garbage-env.test.ts": ["garbage env"],
  "test/cli/run/glob-on-fuse.test.ts": [
    "Bun.Glob.scanSync finds files on FUSE mount",
    "fs.globSync finds files on FUSE mount",
    "fs.readdirSync works on FUSE mount",
    "fs.readdirSync with withFileTypes returns correct types on FUSE mount"
  ],
  "test/cli/run/if-present.test.ts": [
    "should error with missing script",
    "should error with missing module",
    "should error with missing file",
    "should not error with missing script",
    "should not error with missing module",
    "should not error with missing file",
    "should run present script",
    "should run present module"
  ],
  "test/cli/run/jsx-namespaced-attributes.test.ts": ["parses namespaced attributes correctly"],
  "test/cli/run/jsx-symbol-collision.test.ts": [
    "should not have a symbol collision with jsx imports"
  ],
  "test/cli/run/log-test.test.ts": ["should not log .env when quiet", "should log .env by default"],
  "test/cli/run/markdown-entrypoint.test.ts": [
    "renders headings with underlines",
    "renders bold, italic, strikethrough, inline code",
    "renders ordered, unordered, and task lists",
    "renders blockquotes and nested blockquotes",
    "renders horizontal rules",
    "renders fenced code block with JS syntax highlighting",
    "renders fenced code block without language",
    "renders link text with url fallback when no TTY",
    "emits OSC 8 escape for links when hyperlinks are enabled",
    "renders link with text + url pair fallback",
    "renders images as alt text with link",
    "renders wikilinks",
    "falls back to literal text when ",
    "renders simple table with alignment",
    "renders table with CJK multi-width graphemes",
    "renders table with emoji graphemes",
    "renders combining characters without breaking alignment",
    "renders inline formatting inside table cells",
    "renders inline formatting inside headings",
    "nested code span inside bold keeps outer bold",
    "renders mixed inline styles with autolinks",
    "renders nested lists",
    "runs without colors when NO_COLOR is set",
    "renders via ",
    "renders .markdown extension too",
    "wraps inline code spans within COLUMNS, never mid-word",
    "hard-breaks an overlong CJK word at the visible-width boundary",
    "shrinks table columns to fit COLUMNS and wraps cell content",
    "table link with space in URL keeps OSC 8 sequences intact"
  ],
  "test/cli/run/multi-run.test.ts": [
    "runs two scripts in parallel",
    "runs a single script",
    "runs many scripts (10+)",
    "all scripts exit 0",
    "runs .ts files in parallel",
    "runs .js files in parallel",
    "runs file without ./ prefix if it has runnable extension",
    "mixes package.json scripts and file scripts",
    "failure kills other scripts by default",
    "propagates specific non-zero exit code",
    "exit code is from first failed script (handle order)",
    "--no-exit-on-error lets all finish",
    "--no-exit-on-error still reports failure exit code",
    "unknown script falls through to shell (exits non-zero)",
    "each line has prefix label",
    "labels are padded to equal width",
    "multi-line output gets each line prefixed",
    "stderr output is also captured and prefixed",
    "output without trailing newline is flushed on exit",
    "very long output lines are not truncated",
    "empty output script still shows exit status",
    "no color codes when NO_COLOR=1",
    "shows ",
    "lines are not interleaved mid-line",
    "child stdout goes to parent stdout with prefix",
    "child stderr goes to parent stderr with prefix",
    "mixed stdout and stderr go to their respective streams",
    "status messages always go to stderr",
    "runs scripts in order",
    "sequential with single script",
    "sequential stops on first failure",
    "sequential propagates exit code from failed script",
    "sequential --no-exit-on-error continues after failure",
    "sequential file scripts run in order",
    "runs pre, main, post in order",
    "only pre script (no post)",
    "only post script (no pre)",
    "pre failure prevents main and post from running",
    "main failure prevents post from running",
    "parallel: pre/post chained per group, groups run concurrently",
    "sequential: pre/post chained and groups run in order",
    "all pre/post handles share the same label",
    "error when both --parallel and --sequential",
    "error when no script names with --parallel",
    "error when no script names with --sequential",
    "raw commands work without package.json",
    "captures both stdout and stderr",
    "script that produces only stderr output",
    "scripts with pipes work",
    "scripts with environment variables work",
    "scripts with semicolons work",
    "scripts with && work",
    "script names with colons",
    "script names with hyphens",
    "duplicate script names run the script multiple times",
    "scripts that exit immediately",
    "sequential: rapid scripts complete in order",
    "parallel: first handle with non-zero code wins",
    "all zero means exit 0",
    "sequential: exit code of the failed script",
    "scripts run in the package.json directory",
    "handles large number of output lines",
    "handles output from multiple concurrent scripts",
    "runs raw command not in package.json",
    "runs multiple raw commands",
    "mixes raw commands and package.json scripts",
    "later scripts can see files created by earlier scripts",
    "file scripts work without package.json",
    "parallel: fast failure kills a slow script",
    "chunked writes are assembled into complete lines",
    "multiple partial writes coalesce into one line",
    "mixed complete and partial lines",
    "output with only carriage returns (no newline)",
    "empty lines are preserved",
    "parallel: first handle",
    "parallel: all fail, first code wins",
    "sequential: --no-exit-on-error continues through multiple failures",
    "pre failure blocks own group but other groups continue",
    "post script failure is reported correctly",
    "sequential: pre failure with --no-exit-on-error still runs next group",
    "empty script string runs without crashing",
    "script that only writes whitespace",
    "script with very long name",
    "null bytes in output don",
    "very rapid line output doesn",
    "output with unicode characters",
    "Done message appears between sequential scripts",
    "Exited message appears between sequential scripts with --no-exit-on-error",
    "interleaved stdout and stderr are both prefixed",
    "sequential with pre/post creates deep chain that works",
    "parallel with pre/post: failure in one group",
    "more than 6 scripts cycle through colors",
    "build:* matches all build:xxx scripts",
    "* matches all scripts",
    "glob with no matches errors",
    "glob with pre/post scripts",
    "sequential with glob runs matches in alphabetical order",
    "glob mixed with literal script names",
    "--parallel --filter=",
    "--parallel --workspaces matches all workspace packages",
    "--sequential --filter=",
    "workspace + failure aborts other scripts",
    "workspace + --no-exit-on-error lets all finish",
    "--workspaces skips root package",
    "each workspace script runs in its own package directory",
    "multiple script names across workspaces",
    "pre/post scripts work per workspace package",
    "--filter skips packages without the script (no error)",
    "--workspaces errors when a package is missing the script",
    "--workspaces --if-present skips missing scripts silently",
    "labels are padded correctly across workspace packages",
    "package without name field uses relative path as label"
  ],
  "test/cli/run/no-envfile.test.ts": [
    "--no-env-file disables .env loading",
    "--no-env-file disables .env.local loading",
    "--no-env-file disables .env.development.local loading",
    "bunfig env.file = false disables .env loading",
    "bunfig env = false disables .env loading",
    "--no-env-file with -e flag",
    "--no-env-file combined with --env-file still loads explicit file",
    "bunfig env = true still loads .env files",
    "--no-env-file in production mode"
  ],
  "test/cli/run/no-orphans.test.ts": [],
  "test/cli/run/preload-test.test.js": [
    "throws an error when preloaded module fails to execute",
    "throws an error when preloaded module not found",
    "[todo] works",
    "[todo] works from CLI"
  ],
  "test/cli/run/require-and-import-trailing.test.ts": [
    "require() with trailing slash",
    "import() with trailing slash"
  ],
  "test/cli/run/require-cache.test.ts": [
    "require.cache is not an empty object literal when inspected",
    "require.cache",
    "via require() with a lot of long export names",
    "via await import() with a lot of function calls",
    "via import() with a lot of long export names",
    "via require()",
    "via import()"
  ],
  "test/cli/run/run-autoinstall-abs-path.test.ts": [
    "auto-install: DirInfo.abs_path survives threadlocal buffer reuse across resolutions"
  ],
  "test/cli/run/run-autoinstall.test.ts": [
    "${install || ",
    "--install=fallback to install missing packages"
  ],
  "test/cli/run/run-cjs.test.ts": ["running a commonjs module works"],
  "test/cli/run/run-crash-handler.test.ts": [
    "raise ignoring panic handler does not trigger the panic handler",
    "${approach} should report"
  ],
  "test/cli/run/run-eval.test.ts": [
    "it works",
    "import, tsx, require in esm, import.meta",
    "error has source map info 1",
    "process.argv",
    "process._eval",
    "does not crash in non-latin1 directory",
    "eval result between esm imports",
    "→ ${expected}",
    "forced cjs",
    "module, exports, require, __filename, __dirname",
    "module._compile is require(",
    "it gets a correct specifer",
    "it can require",
    "it can import",
    "process._eval (undefined for normal run)"
  ],
  "test/cli/run/run-extensionless.test.ts": ["running extensionless file works"],
  "test/cli/run/run-file-on-fuse.test.ts": ["regular file", "symlink"],
  "test/cli/run/run-process-env.test.ts": [
    "npm_lifecycle_event",
    "npm_lifecycle_event should have the value of the last call"
  ],
  "test/cli/run/run-propagate-sigkill.test.ts": [],
  "test/cli/run/run-quote.test.ts": ["should handle quote escapes"],
  "test/cli/run/run-shell.test.ts": [
    "running a shell script works",
    "invalid syntax reports the error correctly"
  ],
  "test/cli/run/run-unicode.test.ts": [
    "running a weird filename works",
    "ts enum with utf16 works"
  ],
  "test/cli/run/run_command.test.ts": [
    "should error with missing script",
    "an empty-string script value is not a runnable script"
  ],
  "test/cli/run/self-reference.test.ts": ["should resolve self-imports by name"],
  "test/cli/run/shell-keepalive.test.ts": [
    "shell should stay alive while a builtin command is in progress",
    "shell should stay alive while a non-builtin command is in progress"
  ],
  "test/cli/run/sql-preconnect.test.ts": [
    "should attempt to preconnect to PostgreSQL on startup",
    "should not connect when flag is not used"
  ],
  "test/cli/run/syntax.test.ts": [
    "file #${i}: ${JSON.stringify(source)}",
    "eval #${i}: ${JSON.stringify(source)}"
  ],
  "test/cli/run/transpiler-cache.test.ts": [
    "works",
    "works with empty files",
    "ignores files under the minimum cache size",
    "it is indeed content addressable",
    "doing 50 buns at once does not crash",
    "disables the cache instead of falling back to the shared temp directory",
    "works if the cache is not user-readable",
    "works if the cache is not user-writable",
    "does not inline process.env",
    "--feature flag invalidates cache",
    "rejects cached module records containing out-of-range string indices",
    "cached module still works"
  ],
  "test/cli/run/tsconfig-override.test.ts": [
    "should use custom tsconfig for path resolution",
    "should work with relative tsconfig path",
    "should work with monorepo-style paths",
    "should work with nested directories and complex paths",
    "should handle extending tsconfig with overrides",
    "should work from different working directories"
  ],
  "test/cli/run/workspaces.test.ts": [
    "bun run --workspaces runs script in all workspace packages",
    "bun run --workspaces --if-present succeeds when script is missing",
    "bun run --workspaces fails when no packages have the script"
  ],
  "test/cli/test/bun-test.test.ts": [
    "running a non-existent absolute file path is a 1 exit code",
    "can provide no arguments",
    "test #1",
    "test #3",
    "can provide a relative file",
    "${path}",
    "can provide a relative directory",
    "${dir}",
    "${name}",
    "if that filter is a path to a directory, will run all tests in that directory",
    "works with require",
    "works with dynamic import",
    "works with cjs require",
    "works with cjs dynamic import",
    "should not run todo by default",
    "should run todo when enabled",
    "should run nested describe.only",
    "test",
    "should skip non-only tests",
    "test #6",
    "test #8",
    "must provide a number bail",
    "must provide non-negative bail",
    "should not be 0",
    "bail should be 1 by default",
    "test #2",
    "should bail out after 3 failures",
    "test #4",
    "must provide a number timeout",
    "must provide non-negative timeout",
    "timeout can be set to 30ms",
    "ok",
    "timeout",
    "timeout should default to 5000ms",
    "should not group logs by default",
    "should not group logs when disabled",
    "should group logs when enabled",
    "should group logs with multiple files",
    "pass",
    "fail",
    "should group logs with --rerun-each",
    "should not annotate errors by default",
    "should not annotate errors with inspect() by default",
    "inspect",
    "should not annotate errors with inspect() when enabled",
    "should annotate errors in the global scope",
    "should annotate errors with escaped strings",
    "should annotate errors without a stack",
    "should annotate a test timeout",
    "time out",
    "should run tests with test.each",
    "should allow tests run with test.each to be skipped",
    "should allow tests run with test.each to be matched",
    "should run tests with describe.each",
    "addition",
    "check formatting for %i",
    "check formatting for %f",
    "check formatting for %d",
    "check formatting for %s",
    "check formatting for %j",
    "check formatting for %o",
    "check formatting for %#",
    "check formatting for %%",
    "should replace $variables with object properties in test names",
    "should show $variable literal when property doesn",
    "should work with describe.each",
    "has $method",
    "should work with complex property names",
    "should coexist with % formatting for arrays",
    "should support nested property access",
    "should support array indexing with dot notation",
    "handles edge cases with underscores and invalid identifiers",
    "handles deeply nested properties with arrays",
    "handles missing properties gracefully",
    "Prints error when no test matches",
    "Does not print the regex error when a test fails",
    "not-a-test",
    "Does not print the regex error when a test matches and a test passes",
    "path to a non-test.ts file will work",
    "path to a non-test.ts without ./ will print a helpful hint",
    "Skipped and todo tests are filtered out when not matching -t filter",
    "should match filter",
    "should not match filter",
    "another test that should match filter",
    "--tsconfig-override works",
    "--tsconfig-override works with monorepo spec tsconfig",
    "app message",
    "discovers tests in deeply nested directories and prunes dot-dirs and node_modules",
    "a",
    "b",
    "spec",
    "inner",
    "hidden",
    "nm",
    "leaf${i}",
    "scanning a relative subdirectory only runs tests under it",
    "root",
    "most",
    "scanning a relative file path that is not a directory runs that single file",
    "solo",
    "other",
    "[todo] test #2",
    "[todo] can provide an absolute file",
    "[todo] can provide an absolute directory",
    "[todo] can provide a mix of files and directories",
    "[todo] can rerun with a default value",
    "[todo] can rerun with a provided value",
    "[todo] todo",
    "[todo] test #5",
    "[todo] check formatting for %p",
    "[todo] todo test that should not match",
    "[todo] another todo test"
  ],
  "test/cli/test/claudecode-flag.test.ts": [
    "CLAUDECODE=1 shows quiet test output (only failures)",
    "passing test",
    "failing test",
    "CLAUDECODE=1 vs CLAUDECODE=0 comparison",
    "another passing test",
    "CLAUDECODE flag handles no test files found",
    "[todo] todo test"
  ],
  "test/cli/test/concurrent-test-glob.test.ts": [
    "tests matching glob pattern run concurrently",
    "test 1",
    "test 2",
    "test 3",
    "test 4",
    "tests not matching glob pattern run sequentially",
    "sequential test 1",
    "sequential test 2",
    "multiple glob patterns work correctly",
    "concurrent flag overrides concurrent-test-glob"
  ],
  "test/cli/test/coverage.test.ts": [
    "coverage crash",
    "lcov coverage reporter",
    "coverage excludes node_modules directory",
    "coveragePathIgnorePatterns - single pattern string",
    "should call both functions",
    "coveragePathIgnorePatterns - partial coverage without nan",
    "should call only some functions",
    "coveragePathIgnorePatterns - array of patterns",
    "should call all functions",
    "coveragePathIgnorePatterns - glob patterns",
    "coveragePathIgnorePatterns - lcov reporter",
    "coveragePathIgnorePatterns - invalid config type",
    "should pass",
    "coveragePathIgnorePatterns - invalid array item",
    "coveragePathIgnorePatterns - empty array",
    "should call function",
    "coveragePathIgnorePatterns - ignore all files"
  ],
  "test/cli/test/expectations.test.ts": [".toThrow() behaves the same as .toThrow("],
  "test/cli/test/isolation.test.ts": [
    "leak global + server + interval",
    "globalThis is clean",
    "without --isolate, leaked global is visible to next file",
    "with --isolate, each file gets a fresh global",
    "with --isolate, --preload re-runs in each file",
    "preload state present in a",
    "preload state present in b",
    "without --isolate, --preload still runs once (regression)",
    "a",
    "b",
    "with --isolate, module state is not shared between files",
    "bump",
    "fresh",
    "with --isolate, leaked outbound socket is closed before next file",
    "leak a net.Socket",
    "server saw the disconnect",
    "with --isolate, leaked fs.watch is closed before next file",
    "leak an fs.watch",
    "watcher from prior file does not fire",
    "leaked subprocesses are killed for every isolated file, not just the first",
    "leak a sleeper from file A",
    "leak a sleeper from file B",
    "both prior subprocesses were killed by isolation",
    "a sees v1 then rewrites",
    "b sees ${expected}",
    "c (esm import of cjs) sees ${expected}",
    "${name}",
    "t1",
    "t2",
    "leak AbortSignal.timeout",
    "AbortSignal from prior file did not fire here",
    "leak-${i}",
    "fs.watch left open",
    "Bun.serve left running",
    "long setTimeout/setInterval left pending",
    "unwatchFile then free on work pool",
    "require(esm) provider carries module_info"
  ],
  "test/cli/test/parallel.test.ts": [
    "--parallel: each worker has a unique JEST_WORKER_ID and BUN_TEST_WORKER_ID",
    "t",
    "--parallel runs files across workers and aggregates totals",
    "a1",
    "a2",
    "b1",
    "c1",
    "c2",
    "c3",
    "d1",
    "--parallel surfaces failures and exits non-zero",
    "ok",
    "bad",
    "--parallel marks a file whose worker exits mid-run as failed (no retry)",
    "a",
    "b",
    "boom",
    "--parallel without N is accepted and runs all files",
    "--parallel forwards -t to workers",
    "keep_a",
    "drop_a",
    "drop_b",
    "keep_c",
    "drop_d",
    "--parallel --bail stops dispatching new files after threshold",
    "${f}",
    "--parallel prints per-test lines under their file",
    "alpha-one",
    "alpha-two",
    "bravo-one",
    "bravo-two",
    "--parallel streams test results in realtime, not buffered per-file",
    "a-fast",
    "a-slow",
    "b-fast",
    "b-slow",
    "--parallel aggregates failure summary across workers",
    "ok${i}",
    "uniquefail",
    "--parallel --reporter=junit produces a merged report covering all files",
    "ta",
    "tb",
    "tc",
    "--parallel --coverage merges LCOV across workers",
    "--parallel --coverage prints merged text table",
    "--parallel --coverage enforces coverageThreshold with lcov-only reporter",
    "--parallel --dots prints one status character per test",
    "a4",
    "b2",
    "b3",
    "b4",
    "--parallel never interleaves console output across files",
    "${tag}",
    "--parallel lazily scales workers based on file duration",
    "--parallel partitions by directory and steals from the end",
    "--parallel work-stealing balances an uneven directory split",
    "--parallel writes new snapshots from every worker",
    "snap",
    "--parallel: a test producing a >64MB result line is truncated, not treated as a crash",
    "X",
    "--parallel: a test writing garbage to fd 3 does not hang the coordinator",
    "--parallel --randomize without --seed is reproducible via the printed seed",
    "${n}",
    "--parallel forwards --experimental-http2-fetch to workers",
    "h2",
    "--parallel forwards --conditions to workers",
    "--parallel --reporter=junit emits a synthetic suite for crashed files",
    "--parallel: SIGTERM on coordinator kills workers and their grandchildren",
    "slow"
  ],
  "test/cli/test/pass-with-no-tests.test.ts": [
    "--pass-with-no-tests exits with 0 when no test files found",
    "--pass-with-no-tests exits with 0 when filters match no tests",
    "example",
    "without --pass-with-no-tests, exits with 1 when no test files found",
    "without --pass-with-no-tests, exits with 1 when filters match no tests",
    "--pass-with-no-tests still fails when tests fail",
    "failing"
  ],
  "test/cli/test/path-ignore-patterns.test.ts": [
    "bunfig - single pattern string",
    "included test",
    "ignored test",
    "bunfig - array of patterns",
    "main test",
    "helper test",
    "setup test",
    "bunfig - glob pattern with **",
    "unit test",
    "integration test",
    "CLI flag - single pattern",
    "kept test",
    "skipped test",
    "CLI flag - multiple patterns",
    "app test",
    "e2e test",
    "fixture test",
    "bunfig - invalid config type",
    "should pass",
    "bunfig - invalid array item",
    "bunfig - empty array is a no-op",
    "CLI flag overrides bunfig",
    "a test",
    "b test",
    "bare directory name pattern prunes entire subtree",
    "root test",
    "deep ignored test",
    "deeper ignored test"
  ],
  "test/cli/test/rerun-each.test.ts": [
    "--rerun-each should run tests exactly N times",
    "should increment counter",
    "--rerun-each should report correct file count",
    "test in file 1",
    "--rerun-each should handle test failures correctly",
    "fails on second run",
    "--rerun-each resets the toMatchSnapshot() counter between reruns",
    "snap",
    "toMatchSnapshot() counter is reset between per-test retry / repeats",
    "retried",
    "repeated",
    "after"
  ],
  "test/cli/test/retry-flag.test.ts": [
    "--retry retries failed tests",
    "flaky test",
    "per-test { retry } overrides --retry",
    "limited retry",
    "default retry",
    "--retry works with describe blocks and beforeEach/afterEach hooks",
    "flaky with hooks",
    "--retry with multiple tests where only some need retries",
    "always passes",
    "always fails",
    "another passing test",
    "--retry with async tests",
    "async flaky test",
    "--retry with nested describe blocks",
    "deeply nested flaky",
    "--retry past MAX_FLAKY_ATTEMPTS (16) still retries correctly",
    "fails many times",
    "--retry with test.skip and test.todo does not retry them",
    "normal flaky",
    "bunfig.toml retry works equivalently",
    "flaky via bunfig",
    "[todo] todo test"
  ],
  "test/cli/test/test-changed.test.ts": [
    "no changes -> runs nothing and exits 0",
    "direct change to a test file runs only that test",
    "change to a direct dependency selects the importing test",
    "change to a transitive dependency selects the importing test",
    "change to a file no test imports runs nothing",
    "multiple changes select the union of affected tests",
    "shared dependency selects all importers",
    "staged changes are picked up",
    "untracked test file is picked up",
    "--changed=<ref> compares against a commit",
    "--changed=<ref> includes untracked files",
    "change inside node_modules does not select any test",
    "works from a subdirectory of the git repo",
    "untracked test file in a subdirectory is picked up",
    "errors helpfully outside a git repo",
    "test with a syntax-error dependency still filters by changed path",
    "tsconfig paths alias is followed when computing the module graph",
    "restarts and reruns only affected tests when a dependency changes",
    "editing one of several dirty test files reruns only that one"
  ],
  "test/cli/test/test-filter-lifecycle-snapshot.test.ts": ["snapshot"],
  "test/cli/test/test-randomize.test.ts": ["test ${i + 1}"],
  "test/cli/test/test-shard.test.ts": [
    "t",
    "partitions test files across shards with no overlap or gaps",
    "is deterministic across repeated runs",
    "composes with --randomize: shard selection is independent of the seed",
    "--shard=1/1 runs every test file",
    "prints the shard summary line",
    "does not print shard summary when there are no test files",
    "empty shard exits 0 without ",
    "composes with --parallel: shard filters first, then workers run the subset"
  ],
  "test/cli/test/test-timeout-behavior.test.ts": ["[todo] processes get killed"],
  "test/cli/update_interactive_formatting.test.ts": [
    "should handle package names of unusual lengths",
    "should handle version strings of unusual lengths",
    "should truncate extremely long package names",
    "should show workspace column with --filter",
    "should handle catalog dependencies in interactive update",
    "should handle mixed dependency types with various name lengths",
    "should update packages when ",
    "should handle workspace updates with recursive flag",
    "should handle catalog updates correctly",
    "should work correctly when run from inside a workspace directory",
    "should handle basic interactive update with select all",
    "should preserve version prefixes for all semver range types in catalogs",
    "should handle catalog updates in workspaces.catalogs object",
    "should handle mixed workspace and catalog dependencies",
    "should handle selecting specific packages in interactive mode",
    "should handle empty catalog definitions gracefully",
    "should handle cancellation (Ctrl+C) gracefully",
    "should handle packages with pre-release versions correctly",
    "should update catalog in workspaces object (not workspaces.catalogs)",
    "should handle scoped packages in catalogs correctly",
    "should handle catalog updates when running from root with filter",
    "should handle multiple catalog definitions with same package",
    "should handle version ranges with multiple conditions",
    "should handle dry-run mode correctly",
    "should handle keyboard navigation correctly",
    "comprehensive interactive update test with all scenarios",
    "interactive update with workspace filters",
    "interactive update with workspaces.catalogs structure",
    "interactive update dry run mode",
    "should preserve npm: alias prefix when updating packages",
    "interactive update with mixed dependency types"
  ],
  "test/cli/update_interactive_install.test.ts": [
    "should update package.json AND install packages",
    "should work with --latest flag"
  ],
  "test/cli/update_interactive_snapshots.test.ts": [
    "should not crash with various package name lengths",
    "should handle extremely long package names without crashing",
    "should handle complex version strings without crashing"
  ],
  "test/cli/user-agent.test.ts": [
    "custom user agent is sent in HTTP requests",
    "default user agent is used when --user-agent is not specified"
  ],
  "test/cli/watch/watch.test.ts": [],
  "test/cli/watch/watcher-trace.test.ts": [
    "BUN_WATCHER_TRACE creates trace file with watch events",
    "BUN_WATCHER_TRACE with --watch flag",
    "BUN_WATCHER_TRACE with empty path does not create trace",
    "BUN_WATCHER_TRACE appends across reloads"
  ]
}
