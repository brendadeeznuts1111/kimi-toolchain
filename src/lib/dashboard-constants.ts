/**
 * Dashboard constants shared across the dashboard subsystem.
 *
 * Kept in a dedicated module to avoid circular imports through dashboard-settings.ts.
 */

export const CANONICAL_DASHBOARD_PORT = 5678;
